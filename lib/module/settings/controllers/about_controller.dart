import 'package:get/get.dart';

class AboutController extends GetxController {
  // Currently the About screen only contains static information.
  // This controller is kept to maintain the project's GetX architecture.
}
